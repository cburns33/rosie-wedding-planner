import MockupsReview from "@/components/MockupsReview";

export const metadata = {
  title: "Intro mockups — Rosie",
};

export const dynamic = "force-dynamic";

export default function MockupsPage() {
  return <MockupsReview />;
}
